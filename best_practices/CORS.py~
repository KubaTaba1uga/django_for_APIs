"""
        Cross Origin Resource Sharing is used when
                API is axchenging data with different domain
                before it access the web browser.

        This situation is common for backend/frontend
                scenario. If handed unproperly could lead
                to security issues.
"""

# Add to project dependencies
""" django-cors-headers"""

# Add to project settings
INSTALLED_APPS = [
    ...
    'corseheaders' # New
    ...
]
